shader = {
    vs = "picking/vs_picking.sc",
    fs = "picking/fs_picking.sc",
}

uniform = {    
    defines = {
        u_id = {
            name = "u_id",
            type = "v4",    
        }
    }
}

state = "default.state"