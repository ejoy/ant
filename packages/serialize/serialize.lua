return {
    create = require "crypt".uuid,
    watch = require "watch",
    patch = require "patch",
    entity = require "save".entity,
    dl = require "dl",
    stringify = require "stringify",
}
