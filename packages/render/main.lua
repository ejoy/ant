return {
    util = require "util",
    shader_mgr = require "resources.shader_mgr",
    camera = require "camera.util",
    hardware_interface = require "hardware_interface",
    components = require "components.util",
    light = require "light.util",
    geometry = require "geometry"
}
