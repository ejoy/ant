-- this package is designed to server other package, so DO NOT depend any other package, just depend engine and c module
-- currently, this package only used for local, using in runtime will cause some unknown problems
return {
    name = "ant.utility.local",
    entry = "entry"
}
