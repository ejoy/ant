local bloom_path = "/pkg/ant.resources/materials/bloom/"

fx = bloom_path .. "fetch_bright.material"
state = bloom_path .. "postprocess.state"

properties = {
    uniforms = {
        u_bright_threshold = {
            type = "color", name = "bright threshold value", value = {0.75, 0, 0, 0},
        },
    }
}