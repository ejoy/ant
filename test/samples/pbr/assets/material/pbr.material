shader = {
    vs = '/pkg/pbr/assets/shader/pbr/vs_mesh_pbr',
    fs = '/pkg/pbr/assets/shader/pbr/fs_mesh_pbr'
}

state = 'materials/states/default.state'
-- state = {
--    CULL = "CCW",
--    WRITE_MASK = "RGBAZ",
--    DEPTH_TEST = "LESS"
-- }
local deftexpath = '/pkg/ant.resources/materials/textures/default.texture'
local defirrtexpath = '/pkg/ant.resources/materials/textures/defautl_cube.texture'
local deffactor = {1, 1, 1, 1}
properties = {
    textures = {
        s_basecolor 		= {type = 'texture',name = 'base color', 		stage = 0, ref_path = deftexpath},
        s_normal 			= {type = 'texture',name = 'normal', 			stage = 1, ref_path = deftexpath},
        s_metalroughness 	= {type = 'texture',name = 'metal roughness', 	stage = 2, ref_path = deftexpath},
		-- stage=3 for lightmap
		-- stage=4~7 for shadowmaps
		s_emissive			= {type = 'texture',name = 'diffuse irr', 		stage = 8, ref_path = deftexpath},
		s_ambient_occlusion = {type = 'texture',name = 'ambient occlusion', stage = 9, ref_path = deftexpath},
        s_diffuse_irr 		= {type = 'texture',name = 'diffuse irr', 		stage = 10,ref_path = defirrtexpath},
        s_specular_ibl 		= {type = 'texture',name = 'specular ibl', 		stage = 11,ref_path = defirrtexpath},
        s_specular_uv 		= {type = 'texture',name = 'specular uv', 		stage = 12,ref_path = deftexpath},
    },
    uniforms = {
		u_basecolor_factor 	= {type = 'v4', 	name = 'base color factor', default = deffactor},
		u_emissive_factor 	= {type = 'v4', 	name = 'emissive factor', 	default = deffactor},

		--{metal, roughness, normalscale, ao_stength}
		u_param_factor 		= {type = 'v4', 	name = 'param factor', 		default = deffactor},
    }
}
