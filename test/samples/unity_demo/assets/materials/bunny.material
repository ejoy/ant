shader = {
    vs = "//unity_demo/assets/shader/vs_colormesh",  -- 新的shader 修改了，但没有对应的传递颜色
    fs = "//unity_demo/assets/shader/fs_colormesh",  
}

--state = "default.state"
state = {
	CULL = "CW",
	WRITE_MASK = "RGBAZ",
	DEPTH_TEST = "LESS",
	--PT = "POINTS"
}
properties = {
	u_time = {name="u_time", type="v4", value={1, 1, 1, 1}},
}